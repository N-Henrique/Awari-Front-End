console.log("TABUADA");

for (let multiplicador = 1; multiplicador <= 10; multiplicador++) {
  for (let tabuada = 1; tabuada <= 10; tabuada++) {
    const resultado = multiplicador * tabuada;
    console.log(`${multiplicador} * ${tabuada} = ${resultado}`);
  }
}
